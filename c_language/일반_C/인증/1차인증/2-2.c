#include <stdio.h>

int main(){
    char a, b;
    char min, max;
    scanf("%c", &a);
    if ('a' <= a && a <= 'z'){
        max = a;
        min = 'Z';
        while(1){
            scanf("%c", &b);
            if ('a' <= b && b <= 'z' ){
                
            }
        }
    }

    return 0;
}